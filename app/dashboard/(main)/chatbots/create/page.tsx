import { ChatbotCreateWizard } from "@/components/embedded-chat/chatbot-create-wizard";

export const metadata = {
  title: "Nieuwe website-chat",
};

export default function CreateEmbeddedChatbotPage() {
  return <ChatbotCreateWizard />;
}
